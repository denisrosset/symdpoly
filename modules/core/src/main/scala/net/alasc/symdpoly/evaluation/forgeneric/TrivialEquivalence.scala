package net.alasc.symdpoly
package evaluation
/*
import net.alasc.finite.Grp
import shapeless.Witness

/** Equivalence under the adjoint operation. */
final class EigenvalueEvaluator[M <: forgeneric.MonoidDef with Singleton](implicit val witnessM: Witness.Aux[M]) extends Evaluator {

  def apply(mono: M#MonoType): Set[M#MonoType] = Set(mono)
  def compatibleSubgroup(grp: Grp[M#PermutationType]): Grp[M#PermutationType] = grp
  override def isReal: Boolean = true
  override def isCompatibleGroup(grp: Grp[M#PermutationType]): Boolean = true
}
*/